base: rust:slim-trixie
track: main

steps:
  - name: Update
    run: apt update && apt upgrade
  - name: Install git
    run: apt install -y git
  - name: Checkout
    use: cythe-checkout

  - name: Cargo test
    run: cargo test --verbose
