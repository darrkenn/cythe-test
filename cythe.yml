base: rust:slim-trixie
track: main

steps:
  - name: Update
    run: apt-get update
  - name: Install git
    run: apt-get install -y git
  - name: Checkout
    use: cythe-checkout

  - name: Cargo test
    run: cargo test --verbose
