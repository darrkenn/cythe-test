base: rust:slim-trixie
track: main

build_steps:
  - use: cythe-checkout
  - run: cargo build --release
run_steps:
  - name: Cargo test
    run: cargo test
  - name: Run program
    run: ./target/release/cythe-test
    use: cythe-checkout
