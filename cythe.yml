image: darrkenn/cythe-rust:latest
track: main
steps:
  - name: Checkout
    use: cythe-checkout

  - name: Cargo test
    run: cargo test --verbose

  - name: List files
    run: ls -la

  - name: Build
    run: cargo build --release
